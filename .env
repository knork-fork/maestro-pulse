# Absolute path to this maestro-pulse checkout on the host machine. Used by
# the run-manually skill to hand out real, runnable tool paths (see
# README.md). Leave this blank here and set your real value in .env.local
# instead (gitignored), which overrides this file.
MAESTRO_PULSE_HOST_DIR=
